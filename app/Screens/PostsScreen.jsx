import { ScrollView } from "react-native"
import Post from "./components/Post"

const PostsScreen = () => {
    return (
        <ScrollView>
            <Post/>
        </ScrollView>
    )
}

export default PostsScreen